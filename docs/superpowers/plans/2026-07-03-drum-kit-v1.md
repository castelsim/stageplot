# Batteria realistica configurabile nel tool — piano (branch drum-kit-v1)

> executing-plans inline. Mandato Simone 03/07 ("procediamo con l'implementazione della batteria nel software") dopo prototipo approvato (`icon-prototype/preview/drum-configurator.html`).

Scoperta chiave: `COMP.batteria` ha GIÀ defParts/controls dichiarativi (pannello auto-generato, con switch v2.1), `size`, `explode` (pattern timpani). Il modello dati (`it.parts` {toms,floor,hihat,crash,ride,kick2}) NON cambia → retrocompat piena; i doc vecchi vedono il kit ridisegnato realistico (upgrade visivo, posizione invariata).

- [x] Ricognizione: drawBatteria geometrico k=1.32; explodeComposite generico; flattenCssToAttrs copre il PDF; renderer controls a riga 2741+.
- [ ] **T1** Inserire nel template (dopo `var LIB_ICONS = {...};`, con marker `/*DRUM_LIB_ICONS*/`) il blocco `Object.assign(LIB_ICONS,{cassa22,rullante14,tom12,floortom16,hihat_kit,crash16,ride20,sgabellobatt})` generato dagli SVG della libreria (body=`<defs>…</defs><g id>…</g>`, vb=[0,0,W,D]). NB: la chiave `hihat` collide col TYPES hihat? no—LIB_ICONS è namespace separato, ma per chiarezza uso chiave `hihat_kit`.
- [ ] **T2** Sostituire batteriaParts/drawBatteria/sizeBatteria/explodeBatteria con versione a slot (cm reali, prototipo): DRUM_PARTS {k:{icon,w,d}}, drumSlots(p) centri relativi a kick1 (+stool (0,-56), tom rot ±8, crash rot ∓12, offset doppia cassa ±42 e allargamenti dx), bbox→ricentro. draw= `<g translate(cx,cy) rotate(r)>libIcon(icon)</g>`; size=[bbox]; explode→nuovi TYPES catalog:false realistici: kickR,snareR,tomR,floorR,hihatR,crashR,rideR,stoolR (draw libIcon scalato it.w/base → resizable ok; i vecchi kickdrum/tomdrum/piatto restano intatti per compat).
- [ ] **T3** COMP.batteria: defParts+`stool:true`, controls+Sgabello toggle, `presets` [Minimale/Standard/Rock/Jazz]; buildCompCtl: se comp.presets → riga `.chips` in testa (set parts→size→render/save, `.on` se match).
- [ ] **T4** build, verifica Chrome (:8614): piazza batteria (standard realistica), preset Rock/Jazz, toggle+stepper, Dividi→pezzi realistici draggabili, rotazione, dark. Commit.
- [ ] **T5** Test PDF di Simone (click umano) → al suo ok: merge main+push+verify live+memoria.
